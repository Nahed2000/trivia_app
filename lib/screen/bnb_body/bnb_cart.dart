import 'package:flutter/material.dart';

class BnbCart extends StatelessWidget {
  const BnbCart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
