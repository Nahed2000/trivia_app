import 'package:flutter/material.dart';

class BnbQuizzes extends StatelessWidget {
  const BnbQuizzes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
